// Stub for the `server-only` package in the Node test runtime.
// `server-only` exists purely to throw at build time if a server module is
// imported into a client bundle; it has no runtime behavior to preserve here.
export {};
